# Messenger 代码解剖 - 模块地图

## 文件信息
- **源文件**: `assets/App3D-DwM1eiaC.js`
- **总行数**: 76,550
- **类型**: 压缩/混淆的 Three.js 游戏代码

## 核心依赖

### Three.js 基础数学库
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `Quaternion` | 2243-2658 | 旋转/朝向管理 |
| `Vector3` | 2669-2890 | 3D 向量运算 |
| `Sphere` | 4753-4790 | 球体碰撞检测 |
| `Euler` | ~6570+ | 欧拉角 |

### Three.js 相机系统
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `Camera` | 8887-8925 | 基础相机类 |
| `PerspectiveCamera` | 8929-9060 | 透视相机 |
| `OrthographicCamera` | 14912-15255 | 正交相机 |
| `ArrayCamera` | 15285-15350 | 多相机系统 |
| `CubeCamera` | 9063-9253 | 立方体相机 |

### 自定义相机系统
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `baseCamera` | 39977-40150 | 基础相机（BASE_Z = 50） |
| `orbitCamera` | 40202-40575 | 轨道相机控制 |

### 光照与阴影
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `DirectionalLight` | ~25300+ | 方向光 + 阴影相机 |
| `HemisphereLight` | ~25274+ | 半球光 |

### 渲染管线
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `WebGLRenderer` | ~29400+ | WebGL 渲染器 |
| `WebGLBackground` | 21813-21945 | 背景处理 |
| `Raycaster` | 16928-17080 | 射线投射（交互） |

### 后期处理
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `EffectComposer` | ~40900+ | 效果合成器 |
| `RenderPass` | ~41300+ | 渲染通道 |
| `ShaderPass` | ~41175+ | Shader 通道 |
| `DOF` | ~43213+ | 景深效果 |

### 3D 物体
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `Object3D` | ~6100+ | 3D 对象基类 |
| `Mesh` | ~10000+ | 网格 |
| `SkinnedMesh` | ~10600+ | 骨骼动画网格 |
| `InstancedMesh` | ~10024+ | 实例化网格 |
| `BufferGeometry` | ~7900+ | 缓冲几何体 |
| `SphereGeometry` | 13181-13290 | 球体几何 |

### 加载器
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `GLTFLoader` | ~38000+ | glTF 模型加载 |

### 动画系统
| 类名 | 行号范围 | 用途 |
|------|----------|------|
| `QuaternionKeyframeTrack` | 14052-14066 | 四元数关键帧 |
| `AnimationMixer` | ~15600+ | 动画混合器 |
| `AnimationClip` | ~14100+ | 动画片段 |

## 关键常量/变量（推断）

```
BASE_Z = 50                    // 基础相机 Z 位置
CAMERA_TYPE.PERSPECTIVE        // 透视模式
CAMERA_TYPE.ORTHOGRAPHIC       // 正交模式
```

## 输入系统

| 事件类型 | 行号范围 | 用途 |
|----------|----------|------|
| `pointermove` | 33756 | 触摸/鼠标移动 |
| `pointerdown/up` | 34480-34500 | 触摸/鼠标按下/释放 |
| `wheel` | 34304 | 滚轮缩放 |
| `keydown/keyup` | 39775-39776 | 键盘输入 |

## 渲染循环结构（推断）

```javascript
// 1. 更新相机 (update camera)
this.camera.updateMatrixWorld()

// 2. 射线投射 (raycasting) for interaction
this._raycaster.setFromCamera()

// 3. 渲染场景
webgl.render(scene, camera)

// 4. 后期处理
composer.render()
```

## 架构推测

```
┌─────────────────────────────────────────────┐
│                 index.html                   │
│    (入口, 加载 App3D-DwM1eiaC.js)           │
└─────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│           WebGLRenderer / Scene             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │  星球   │  │ Avatar  │  │   NPC   │    │
│  │ (Planet)│  │ (玩家)  │  │  (NPC)  │    │
│  └─────────┘  └─────────┘  └─────────┘    │
└─────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│        EffectComposer (后期处理)            │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │  Bloom │  │  DOF    │  │  ...    │    │
│  └─────────┘  └─────────┘  └─────────┘    │
└─────────────────────────────────────────────┘
```

## 关键词频率统计（从 key-symbols.txt）

| 关键词 | 出现次数（估计） | 主要用途 |
|--------|-----------------|----------|
| `Vector3` | ~200+ | 向量/位置运算 |
| `camera` | ~150+ | 相机控制 |
| `Quaternion` | ~80+ | 旋转 |
| `Sphere` | ~50+ | 碰撞/包围球 |
| `Raycaster` | ~30+ | 交互检测 |
| `remove` | 频繁 | DOM/对象清理 |

## 第三方库（识别）

1. **Three.js r152+** (根据代码特征)
2. **Draco** (几何压缩, dracoworker)
3. **KTX2** (纹理格式)
4. **Basis Universal** (跨平台纹理)

## 备注

- 代码经过混淆压缩，变量名被替换为短名称
- 使用 ES6 模块但被 webpack 或类似工具打包
- 包含大量内联着色器代码（GLSL）
- 游戏逻辑部分需要进一步分析 App3D-BLRWK1h9.js
